t_killer {
	killerid INT +PK,
	name VARCHAR(50) !UNIQUE,
	killed INT,
	last VARCHAR(50)
}

t_killer_re {
	killer VARCHAR(50),
	rid  INT
}

t_resident{
	id INT +PK,
	name VARCHAR(50) !UNIQUE,
}